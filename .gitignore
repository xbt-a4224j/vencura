node_modules/
dist/
build/
coverage/
.turbo/
*.log
.DS_Store

# secrets — NEVER commit
.env
.env.*
!.env.example

# local db / prisma
*.db
prisma/dev.db*

# JetBrains / WebStorm
.idea/
*.iml

# Vercel link artifact (org/project ids) — never commit
.vercel/

# Local build notes / learning aids — kept locally, not part of the deliverable
docs/study/
CLAUDE.md
DEVLOG.md
tickets.md
docs/METHODOLOGY.md

# Unrequested design/plan docs — brainstorming stays in chat, not committed
docs/specs/
docs/plans/
docs/superpowers/

# Local app-standup runtime (pids + logs)
.standup/
.vercel

# stray local screenshots / scratch images (not deliverables)
img_*.png
Screenshot*.png
# but include walkthrough screenshots in public/
!packages/web/public/*.png
